{
  "name": "@ogrtk/kintone-api-plugin-uploader",
  "description": "Uplodes kintone plugin through kintone rest api",
  "version": "0.8.0",
  "type": "module",
  "bin": {
    "kintone-api-plugin-uploader": "./dist/cli.js"
  },
  "files": ["dist", "node_modules", "README.md"],
  "scripts": {
    "check": "biome check",
    "build": "npm run check && tsc --noEmit && tsx esbuild.config.ts",
    "startts": "export $(cat .local | xargs) && tsx ./src/cli.ts -f ./testdata/plugin.zip -i ./testdata/pluginId.txt",
    "start": "export $(cat .local | xargs) && node ./dist/cli.js -f ./testdata/plugin.zip -i ./testdata/pluginId.txt"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^22.10.5",
    "@types/yargs": "^17.0.33",
    "esbuild": "^0.24.2",
    "tsx": "^4.19.2"
  },
  "dependencies": {
    "@kintone/rest-api-client": "^5.6.0",
    "yargs": "^17.7.2"
  }
}
